// JavaScript Document
new WOW().init(); 

/*//检验浏览器是否支持
function IsBrowerSupport(){
	if(window.sessionStorage){
		
	}else{
		alert("您的浏览器不支持 sessionStorage!");
	}
}
	

$(function(){
	IsBrowerSupport();
	
	//获取客户端用户名
	var userName= window.sessionStorage.userName;
	if(userName){
		$("#div_user").show();
		$("#span_username").text(userName);
	}else{
		$("#div_login").show(500);
		return ;	
	}
	
	//用户退出
	$("#loginOut").bind('click',function(){
		window.sessionStorage.clear();
		location.reload();
	});
	
});*/